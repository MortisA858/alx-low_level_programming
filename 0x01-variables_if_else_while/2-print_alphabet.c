#include <stdio.h>

/**
 * main - Entry point
 *
 * Return : Accept 0 (Success)
 */

int main(void)
{
	char x = 26
	int i = 0

	while (i < 26; x++; i++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}

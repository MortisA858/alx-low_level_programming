#ifndef MAIN_H
#define MAIN_H

/**
 * function-like macro ABS(x) that computes the absolute value of a number x
 * define - define macro
 * @ABS: macro
 * @x: value
 */

#define ABS(x) ((x) < (0) ? -(x) : (x))

#endif
